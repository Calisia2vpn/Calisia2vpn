{
  "name": "calisia2vpn-backend",
  "version": "0.5.0",
  "private": true,
  "description": "Backend scaffold for Android launch (auth + subscription validation + webhooks)",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "check": "node --check src/server.js && node --check src/ai.js"
  },
  "engines": {
    "node": ">=20"
  }
}
