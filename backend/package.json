{
  "name": "calisia2vpn-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Production backend for Calisia2vpn with PostgreSQL, Redis-ready architecture, auth, subscriptions and webhooks",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "migrate": "node --env-file-if-exists=.env src/db/migrate.js",
    "check": "node --check src/server.js && node --check src/ai.js && node --check src/db/index.js && node --check src/db/migrate.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
