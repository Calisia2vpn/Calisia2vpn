window.__APP_CONFIG = {
  // In production behind nginx reverse proxy, keep this as '/api'.
  // If backend is on another domain, set full URL like 'https://api.example.com'.
  API_BASE_URL: '/api'
};
